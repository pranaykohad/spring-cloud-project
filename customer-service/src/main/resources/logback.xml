<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <property name="LOG_FILE" value="logs/app.log"/>

     <!-- Rolling File Appender -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Active log file -->
        <file>event-service.log</file>
        
        <!-- File Rolling Policy -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- File pattern for rolled logs -->
            <fileNamePattern>logs/event-service-%d{yyyy-MM-dd}-%i.log.gz</fileNamePattern>
            <!-- Max history of rolled logs -->
            <maxHistory>30</maxHistory>
            <!-- Limit total size of all log files -->
            <totalSizeCap>3GB</totalSizeCap>
            <!-- Trigger rolling by size (in addition to time) -->
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>

        <!-- Log message pattern -->
        <encoder>
           <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg %X{traceId:-} %X{spanId:-} %n</pattern>
        </encoder>
    </appender>

	<!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
           <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </layout>
    </appender>
    

    <root level="info" additivity="false">
        <appender-ref ref="RollingFile"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>